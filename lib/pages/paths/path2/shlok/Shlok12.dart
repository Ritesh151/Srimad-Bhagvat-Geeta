import 'package:flutter/material.dart';

class Shlok12 extends StatelessWidget {
  const Shlok12({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 12'),
      ),
      body: const Center(
        child: Text('Content of Shlok 12 goes here.'),
      ),
    );
  }
}
