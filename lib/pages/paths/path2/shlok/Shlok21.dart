import 'package:flutter/material.dart';

class Shlok21 extends StatelessWidget {
  const Shlok21({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 21'),
      ),
      body: const Center(
        child: Text('Content of Shlok 21 goes here.'),
      ),
    );
  }
}
