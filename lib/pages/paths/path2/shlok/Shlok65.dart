import 'package:flutter/material.dart';

class Shlok65 extends StatelessWidget {
  const Shlok65({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 65'),
      ),
      body: const Center(
        child: Text('Content of Shlok 65 goes here.'),
      ),
    );
  }
}
