import 'package:flutter/material.dart';

class Shlok3 extends StatelessWidget {
  const Shlok3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 3'),
      ),
      body: const Center(
        child: Text('Content of Shlok 3 goes here.'),
      ),
    );
  }
}
