import 'package:flutter/material.dart';

class Shlok4 extends StatelessWidget {
  const Shlok4({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 4'),
      ),
      body: const Center(
        child: Text('Content of Shlok 4 goes here.'),
      ),
    );
  }
}
