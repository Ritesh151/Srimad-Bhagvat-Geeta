import 'package:flutter/material.dart';

class Shlok64 extends StatelessWidget {
  const Shlok64({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 64'),
      ),
      body: const Center(
        child: Text('Content of Shlok 64 goes here.'),
      ),
    );
  }
}
