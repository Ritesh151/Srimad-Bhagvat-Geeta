import 'package:flutter/material.dart';

class Shlok6 extends StatelessWidget {
  const Shlok6({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 6'),
      ),
      body: const Center(
        child: Text('Content of Shlok 6 goes here.'),
      ),
    );
  }
}
