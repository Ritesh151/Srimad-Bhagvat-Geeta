import 'package:flutter/material.dart';

class Shlok8 extends StatelessWidget {
  const Shlok8({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 8'),
      ),
      body: const Center(
        child: Text('Content of Shlok 8 goes here.'),
      ),
    );
  }
}
