import 'package:flutter/material.dart';

class Shlok25 extends StatelessWidget {
  const Shlok25({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 25'),
      ),
      body: const Center(
        child: Text('Content of Shlok 25 goes here.'),
      ),
    );
  }
}
