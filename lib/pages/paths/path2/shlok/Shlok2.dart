import 'package:flutter/material.dart';

class Shlok2 extends StatelessWidget {
  const Shlok2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 2'),
      ),
      body: const Center(
        child: Text('Content of Shlok 2 goes here.'),
      ),
    );
  }
}
