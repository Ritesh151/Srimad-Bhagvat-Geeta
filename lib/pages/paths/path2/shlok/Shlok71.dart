import 'package:flutter/material.dart';

class Shlok71 extends StatelessWidget {
  const Shlok71({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 71'),
      ),
      body: const Center(
        child: Text('Content of Shlok 71 goes here.'),
      ),
    );
  }
}
