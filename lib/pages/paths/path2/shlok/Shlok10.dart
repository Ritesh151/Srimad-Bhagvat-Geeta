import 'package:flutter/material.dart';

class Shlok10 extends StatelessWidget {
  const Shlok10({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 10'),
      ),
      body: const Center(
        child: Text('Content of Shlok 10 goes here.'),
      ),
    );
  }
}
