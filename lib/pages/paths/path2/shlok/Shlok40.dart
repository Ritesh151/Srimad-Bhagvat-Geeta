import 'package:flutter/material.dart';

class Shlok40 extends StatelessWidget {
  const Shlok40({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 40'),
      ),
      body: const Center(
        child: Text('Content of Shlok 40 goes here.'),
      ),
    );
  }
}
