import 'package:flutter/material.dart';

class Shlok72 extends StatelessWidget {
  const Shlok72({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 72'),
      ),
      body: const Center(
        child: Text('Content of Shlok 72 goes here.'),
      ),
    );
  }
}
