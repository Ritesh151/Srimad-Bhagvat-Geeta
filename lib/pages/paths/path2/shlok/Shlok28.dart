import 'package:flutter/material.dart';

class Shlok28 extends StatelessWidget {
  const Shlok28({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 28'),
      ),
      body: const Center(
        child: Text('Content of Shlok 28 goes here.'),
      ),
    );
  }
}
