import 'package:flutter/material.dart';

class Shlok9 extends StatelessWidget {
  const Shlok9({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 9'),
      ),
      body: const Center(
        child: Text('Content of Shlok 9 goes here.'),
      ),
    );
  }
}
