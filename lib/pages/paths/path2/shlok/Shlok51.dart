import 'package:flutter/material.dart';

class Shlok51 extends StatelessWidget {
  const Shlok51({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 51'),
      ),
      body: const Center(
        child: Text('Content of Shlok 51 goes here.'),
      ),
    );
  }
}
