import 'package:flutter/material.dart';

class Shlok14 extends StatelessWidget {
  const Shlok14({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 14'),
      ),
      body: const Center(
        child: Text('Content of Shlok 14 goes here.'),
      ),
    );
  }
}
