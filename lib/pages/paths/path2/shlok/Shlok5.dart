import 'package:flutter/material.dart';

class Shlok5 extends StatelessWidget {
  const Shlok5({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 5'),
      ),
      body: const Center(
        child: Text('Content of Shlok 5 goes here.'),
      ),
    );
  }
}
