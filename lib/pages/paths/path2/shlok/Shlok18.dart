import 'package:flutter/material.dart';

class Shlok18 extends StatelessWidget {
  const Shlok18({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 18'),
      ),
      body: const Center(
        child: Text('Content of Shlok 18 goes here.'),
      ),
    );
  }
}
