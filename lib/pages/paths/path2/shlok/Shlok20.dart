import 'package:flutter/material.dart';

class Shlok20 extends StatelessWidget {
  const Shlok20({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 20'),
      ),
      body: const Center(
        child: Text('Content of Shlok 20 goes here.'),
      ),
    );
  }
}
