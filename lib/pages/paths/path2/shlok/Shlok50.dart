import 'package:flutter/material.dart';

class Shlok50 extends StatelessWidget {
  const Shlok50({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shlok 50'),
      ),
      body: const Center(
        child: Text('Content of Shlok 50 goes here.'),
      ),
    );
  }
}
